# Spring Boot Reactive Web Sandbox

## Getting Started

1. Run the service:

    ```shell
    clear; mvn spring-boot:run
    ```

2. Make a GET request:

    ```shell
    clear; curl -s localhost:8080/hello
    ```
